Create Database tarea4;
use tarea4;

create table vote(
    id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    album VARCHAR(100) NOT NULL,
    year varchar(100) NOT NULL,
    _rank varchar(100) NOT NULL
);
